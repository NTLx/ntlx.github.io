---
title: AI 行业洞察
description: 商业模型、公司战略、产业经济学、行业趋势
lastUpdated: false
sidebar:
  label: AI 行业洞察
  order: 0
---

import { getCollection } from 'astro:content';
import ArticleList from '../../../components/ArticleList.astro';

export const posts = (await getCollection(
  'docs',
  ({ id, data }) => id.startsWith('articles/') && data.category === 'ai-industry',
))
  .sort((a, b) => +(b.data.date ?? 0) - +(a.data.date ?? 0));

商业模型、公司战略、产业经济学、行业趋势。共 **{posts.length} 篇**，按发布时间倒序排列。

<ArticleList posts={posts} showCategory={false} />
