---
title: Agent 与工具链
description: Agent 架构设计、MCP 协议、测试验证、记忆系统
lastUpdated: false
sidebar:
  label: Agent 与工具链
  order: 0
---

import { getCollection } from 'astro:content';
import ArticleList from '../../../components/ArticleList.astro';

export const posts = (await getCollection(
  'docs',
  ({ id, data }) => id.startsWith('articles/') && data.category === 'ai-agents',
))
  .sort((a, b) => +(b.data.date ?? 0) - +(a.data.date ?? 0));

Agent 架构设计、MCP 协议、测试验证、记忆系统。共 **{posts.length} 篇**，按发布时间倒序排列。

<ArticleList posts={posts} showCategory={false} />
