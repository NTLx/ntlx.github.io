---
title: AI 编程实践
description: AI 编码工具、工程方法论、开发提效与技术债务
lastUpdated: false
sidebar:
  label: AI 编程实践
  order: 0
---

import { getCollection } from 'astro:content';
import ArticleList from '../../../components/ArticleList.astro';

export const posts = (await getCollection(
  'docs',
  ({ id, data }) => id.startsWith('articles/') && data.category === 'ai-coding',
))
  .sort((a, b) => +(b.data.date ?? 0) - +(a.data.date ?? 0));

AI 编码工具、工程方法论、开发提效与技术债务。共 **{posts.length} 篇**，按发布时间倒序排列。

<ArticleList posts={posts} showCategory={false} />
