---
title: 专题
description: 按主题浏览 NTLx's Blog 的全部文章
lastUpdated: false
sidebar:
  hidden: true
---

import { getCollection } from 'astro:content';
import TopicOverview from '../../components/TopicOverview.astro';

export const articles = await getCollection(
  'docs',
  ({ id, data }) => id.startsWith('articles/') && Boolean(data.date && data.category),
);

博客文章目前按六个长期主题组织。专题页只做发现入口，不改变任何文章 URL，也不要求额外维护。

<TopicOverview articles={articles} />
