---
title: 技术笔记
description: 按问题和工具组织的长期维护型技术 Reference
lastUpdated: false
sidebar:
  hidden: true
---

import { getCollection } from 'astro:content';
import NotesOverview from '../../components/NotesOverview.astro';

export const docs = await getCollection(
  'docs',
  ({ id }) => !id.startsWith('articles/') && !['index', 'archive', 'topics', 'notes', 'about', '404'].includes(id),
);

技术笔记与博客文章分开：这里更接近长期维护的 Reference，适合按问题、工具和环境查找，而不是按发布时间阅读。顶部搜索可以跨博客与技术笔记全文检索。

<NotesOverview docs={docs} />
