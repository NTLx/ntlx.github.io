<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://ntlx.github.io</id>
    <title>NTLx Blog</title>
    <updated>2019-11-13T01:15:42.324Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="https://ntlx.github.io"/>
    <link rel="self" href="https://ntlx.github.io/atom.xml"/>
    <subtitle>Play with music</subtitle>
    <logo>https://ntlx.github.io/images/avatar.png</logo>
    <icon>https://ntlx.github.io/favicon.ico</icon>
    <rights>All rights reserved 2019, NTLx Blog</rights>
    <entry>
        <title type="html"><![CDATA[About CentOS 8]]></title>
        <id>https://ntlx.github.io/post/about-centos-8</id>
        <link href="https://ntlx.github.io/post/about-centos-8">
        </link>
        <updated>2019-11-13T01:03:17.000Z</updated>
        <summary type="html"><![CDATA[<p>CentOS 8 Installation Note &amp; Some usefull tips for now.</p>
]]></summary>
        <content type="html"><![CDATA[<p>CentOS 8 Installation Note &amp; Some usefull tips for now.</p>
<!-- more -->
<figure data-type="image" tabindex="1"><img src="https://lx-public-pic.oss-cn-shanghai.aliyuncs.com/PicGo/20191113091528.png" alt="logo"></figure>
<h1 id="installation-note">Installation Note</h1>
<blockquote>
<p>for Minimal Installation</p>
</blockquote>
<h2 id="system-dependency">system dependency</h2>
<pre><code class="language-bash">sudo yum install autoconf automake c-ares c-ares-devel chrony clang clang-devel clang-libs clang-tools-extra cmake cmake-data cmake-filesystem cmake-rpm-macros CUnit curl expat expat-devel gcc gcc-c++ gmp gmp-c++ gmp-devel gnutls gnutls-c++ gnutls-devel gnutls-utils graphviz icu libconfig libgcc libgcrypt libgcrypt-devel libicu libicu-devel libpkgconf libssh2 libstdc++ libstdc++-devel libuv libxml2 libxml2-devel make make-devel ncurses-c++-libs nettle nettle-devel nodejs nodejs-devel openssh-clients openssh-server openssl openssl-devel openssl-libs pcre pcre2 pcre2-devel pcre-cpp pcre-devel perl-CPAN pkgconf pkgconf-pkg-config policycoreutils postfix sqlite-devel sqlite-libs zlib zlib-devel wget tar git
</code></pre>
<h2 id="set-language">set language</h2>
<p>add blow to <code>~/.bashrc</code> or <code>~/.zshrc</code></p>
<pre><code class="language-bash">export LANGUAGE=en_US.utf8
export LC_ALL=en_US.utf8
export LANG=en_US.utf8
</code></pre>
<h2 id="install-tmux-zsh">install tmux zsh</h2>
<pre><code class="language-bash">sudo yum install tmux zsh
</code></pre>
<h2 id="install-python3">install python3</h2>
<pre><code class="language-bash">sudo yum install python36 python36-devel
</code></pre>
<h2 id="install-shadowsocks">install shadowsocks</h2>
<pre><code class="language-bash">sudo pip3 install shadowsocks-py
sudo ln -s /usr/local/bin/sslocal /usr/bin/
</code></pre>
<h3 id="using-shadowsocks">using shadowsocks</h3>
<pre><code class="language-bash">sudo sslocal -c HK03.json -d start
</code></pre>
<h2 id="install-privoxy">install privoxy</h2>
<p><a href="https://sourceforge.net/projects/ijbswa/files/Sources/">Download</a></p>
<pre><code class="language-bash">tar zxf privoxy-3.0.28-stable-src.tar.gz
cd privoxy-3.0.28-stable/
sudo autoheader
sudo autoconf
sudo ./configure
sudo make
sudo useradd privoxy -r -s /usr/sbin/nologin
sudo make install
</code></pre>
<h2 id="install-aria2-v1350">install aria2 v1.35.0</h2>
<pre><code class="language-bash">wget https://github.com/aria2/aria2/releases/download/release-1.35.0/aria2-1.35.0.tar.gz
tar zxf aria2-1.35.0.tar.gz
cd aria2-1.35.0
./configure --prefix=$HOME/software/aria2-1.35.0
make
make install
</code></pre>
<h3 id="install-aria2-webui">install aria2 webui</h3>
<pre><code class="language-bash">git clone git@github.com:ziahamza/webui-aria2.git
# or wget https://github.com/ziahamza/webui-aria2/archive/master.zip
</code></pre>
<h3 id="run-aria2">run aria2</h3>
<pre><code class="language-bash">~/software/aria2-1.35.0/bin/aria2c --enable-rpc --rpc-listen-all
</code></pre>
<h3 id="run-webui">run webui</h3>
<pre><code class="language-bash">node node-server.js
</code></pre>
<h2 id="install-ruby">install ruby</h2>
<pre><code class="language-bash">sudo yum install ruby ruby-devel ruby-libs
gem sources --remove https://rubygems.org/
gem sources -a https://mirrors.aliyun.com/rubygems/
gem update
gem update --system
</code></pre>
<p>edit <code>~/.gemrc</code> like blow:</p>
<pre><code>---
:backtrace: false
:bulk_threshold: 1000
:sources:
- https://mirrors.aliyun.com/rubygems/
:update_sources: true
:verbose: true
gem: &quot;--user-install --bindir /home/lx/bin&quot;
:concurrent_downloads: 8
install: --no-document
update: --no-document
</code></pre>
<h2 id="install-samba4">Install Samba4</h2>
<pre><code class="language-bash">sudo dnf install samba samba-client samba-common
sudo systemctl start smb
sudo systemctl enable smb
sudo systemctl status smb
sudo firewall-cmd --permanent --add-service=samba
sudo firewall-cmd --reload
cp /etc/samba/smb.conf /etc/samba/smb.conf.orig
sudo touch /etc/samba/smbpasswd
sudo vim /etc/samba/smb.conf
testparm
# groupadd smbgrp
# usermod lx -aG smbgrp
sudo smbpasswd -a lx
sudo systemctl restart smb
sudo systemctl restart nmb
</code></pre>
<blockquote>
<p>正常使用 Samba 服务必须设置好 SELinux，一般<s>简单粗暴的</s>直接关闭，但其实设置好更能保证安全性，可参考<a href="https://linuxconfig.org/install-samba-on-redhat-8#h7-setup-selinux-for-samba">这里</a>进行设置。</p>
</blockquote>
<h3 id="minimal-config">minimal config</h3>
<pre><code class="language-conf">[global]
    workgroup = WORKGROUP
    security = user
    netbios name = HaseeServer
    hosts allow = 192.168.0.
    passdb backend = smbpasswd
    encrypt passwords = true
    smb passwd file = /etc/samba/smbpasswd
    log file = /var/log/samba/log.%m
    max open files = 1000

[SSD]
    comment = Hasee SSD Download folder
    path = /home/lx/Download
    valid users = lx
    writable = no
    browseable = yes
    available = yes

[SATA]
    comment = Hasee SATA Download folder
    path = /home/lx/public/Download
    valid users = lx
    writeable = no
    browseable = yes
    available = yes
</code></pre>
<h1 id="usefull-tips">Usefull Tips</h1>
<h2 id="about-disk">about disk</h2>
<pre><code class="language-bash">sudo yum install gdisk
</code></pre>
<pre><code class="language-bash">sudo gdisk /dev/sda
sudo mkfs.xfs -f -d agcount=8 /dev/sda1
sudo mount /dev/sda1 /store
</code></pre>
<h2 id="sync-time">sync time</h2>
<pre><code class="language-bash">sudo systemctl enable chronyd                  # 设置开机启动
sudo systemctl is-enabled chronyd              # 查看是否设置了开机启动
sudo systemctl start chronyd                   # 开启 chrony
sudo timedatectl list-timezones                # 列出所有时区
sudo timedatectl set-local-rtc 1               # 将硬件时钟调整为与本地时钟一致，0 即设置为 UTC 时间
sudo timedatectl set-timezone Asia/Shanghai    # 设置系统时区为上海
sudo timedatectl set-ntp yes                   # 设置开启自动同步
</code></pre>
<h2 id="about-selinux">about SELinux</h2>
<pre><code class="language-bash">getenforce                      # 查看运行状态，如果显示 Enforcing，说明是开启状态
sudo setenforce 0               # 临时禁用 SELinux
sudo vim /etc/selinux/config    # 编辑配置文件，永久禁用 SELinux
</code></pre>
<p>将 SELINUX=enforcing 修改为 SELINUX=disabled 即可：</p>
<pre><code class="language-conf"># This file controls the state of SELinux on the system.
# SELINUX= can take one of these three values:
#     enforcing - SELinux security policy is enforced.
#     permissive - SELinux prints warnings instead of enforcing.
#     disabled - No SELinux policy is loaded.
SELINUX=enforcing
# SELINUXTYPE= can take one of these three values:
#     targeted - Targeted processes are protected,
#     minimum - Modification of targeted policy. Only selected processes are protected.
#     mls - Multi Level Security protection.
SELINUXTYPE=targeted
</code></pre>
<h2 id="about-firewall">about firewall</h2>
<h3 id="systemctl">systemctl</h3>
<pre><code class="language-bash">sudo systemctl unmask firewalld                # 执行命令，即可实现取消服务的锁定
sudo systemctl mask firewalld                  # 下次需要锁定该服务时执行
sudo systemctl start firewalld.service         # 启动防火墙
sudo systemctl stop firewalld.service          # 停止防火墙
sudo systemctl reloadt firewalld.service       # 重载配置
sudo systemctl restart firewalld.service       # 重启服务
sudo systemctl status firewalld.service        # 显示服务的状态
sudo systemctl enable firewalld.service        # 在开机时启用服务
sudo systemctl disable firewalld.service       # 在开机时禁用服务
sudo systemctl is-enabled firewalld.service    # 查看服务是否开机启动
sudo systemctl list-unit-files|grep enabled    # 查看已启动的服务列表
sudo systemctl --failed                        # 查看启动失败的服务列表
</code></pre>
<h3 id="firewall-cmd">firewall-cmd</h3>
<pre><code class="language-bash">sudo firewall-cmd --state            # 查看防火墙状态
sudo firewall-cmd --reload           # 更新防火墙规则
sudo firewall-cmd --state            # 查看防火墙状态
sudo firewall-cmd --reload           # 重载防火墙规则
sudo firewall-cmd --list-ports       # 查看所有打开的端口
sudo firewall-cmd --list-services    # 查看所有允许的服务
sudo firewall-cmd --get-services     # 获取所有支持的服务

# 区域相关
sudo firewall-cmd --list-all-zones                      # 查看所有区域信息
sudo firewall-cmd --get-active-zones                    # 查看活动区域信息
sudo firewall-cmd --set-default-zone=public             # 设置 public 为默认区域
sudo firewall-cmd --get-default-zone                    # 查看默认区域信息
sudo firewall-cmd --zone=public --add-interface=eth0    # 将接口 eth0 加入区域 public

# 接口相关
sudo firewall-cmd --zone=public --remove-interface=eth0     # 从区域 public 中删除接口 eth0
sudo firewall-cmd --zone=default --change-interface=eth0    # 修改接口 eth0 所属区域为 default
sudo firewall-cmd --get-zone-of-interface=eth0              # 查看接口 eth0 所属区域

# 端口控制
sudo firewall-cmd --add-port=80/tcp --permanent                            # 永久添加 80 端口例外(全局)
sudo firewall-cmd --remove-port=80/tcp --permanent                         # 永久删除 80 端口例外(全局)
sudo firewall-cmd --add-port=65001-65010/tcp --permanent                   # 永久增加 65001-65010 例外(全局)
sudo firewall-cmd  --zone=public --add-port=80/tcp --permanent             # 永久添加 80 端口例外(区域public)
sudo firewall-cmd  --zone=public --remove-port=80/tcp --permanent          # 永久删除 80 端口例外(区域public)
sudo firewall-cmd  --zone=public --add-port=65001-65010/tcp --permanent    # 永久增加 65001-65010 例外(区域 public)
sudo firewall-cmd --query-port=8080/tcp                                    # 查询端口是否开放
sudo firewall-cmd --permanent --add-port=80/tcp                            # 开放 80 端口
sudo firewall-cmd --permanent --remove-port=8080/tcp                       # 移除端口
sudo firewall-cmd --reload                                                 # 重启防火墙(修改配置后要重启防火墙)
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Shadowsocks科学上网]]></title>
        <id>https://ntlx.github.io/post/shadowsocks-ke-xue-shang-wang</id>
        <link href="https://ntlx.github.io/post/shadowsocks-ke-xue-shang-wang">
        </link>
        <updated>2019-11-12T10:15:12.000Z</updated>
        <summary type="html"><![CDATA[<p><span id="top"></span></p>
<h1 id="shadowsocks">Shadowsocks</h1>
<p>Shadowsocks 可以指：一种基于 Socks5 代理方式的加密传输协议，也可以指实现这个协议的各种传输包，并采用 Apache 许可证、GPL、MIT 许可证等多种自由软件许可协议开放源代码。shadowsocks 分为服务器端和客户端，在使用之前，需要先将服务器端部署到服务器上面，然后通过客户端连接并创建本地代理。目前包使用 Python、C、C++、C#、Go 语言等编程语言开发。<a href="https://zh.wikipedia.org/wiki/Shadowsocks">Wikipedia</a></p>
]]></summary>
        <content type="html"><![CDATA[<p><span id="top"></span></p>
<h1 id="shadowsocks">Shadowsocks</h1>
<p>Shadowsocks 可以指：一种基于 Socks5 代理方式的加密传输协议，也可以指实现这个协议的各种传输包，并采用 Apache 许可证、GPL、MIT 许可证等多种自由软件许可协议开放源代码。shadowsocks 分为服务器端和客户端，在使用之前，需要先将服务器端部署到服务器上面，然后通过客户端连接并创建本地代理。目前包使用 Python、C、C++、C#、Go 语言等编程语言开发。<a href="https://zh.wikipedia.org/wiki/Shadowsocks">Wikipedia</a></p>
<!-- more -->
<p>Shadowsocks 的运行原理与其他代理工具基本相同，使用特定的中转服务器完成数据传输。</p>
<p>在服务器端部署完成后，用户需要按照指定的密码、加密方式和端口，使用客户端软件与其连接。在成功连接到服务器后，客户端会在用户的电脑上构建一个本地Socks5代理。浏览网络时，网络流量会被分到本地socks5代理，客户端将其加密之后发送到服务器，服务器以同样的加密方式将流量回传给客户端，以此实现代理上网。<a href="https://zh.wikibooks.org/wiki/Shadowsocks">维基教科书</a></p>
<h2 id="software-app">Software &amp; App</h2>
<p>Shadowsocks 支持广泛、版本众多，这里仅介绍常用平台客户端。</p>
<blockquote>
<p>App 安装时请确保系统为当前流行的最新版本，否则可能遇到依赖缺失甚至兼容性问题</p>
</blockquote>
<ul>
<li>
<p>Windows<br>
<a href="https://github.com/shadowsocks/shadowsocks-windows"><code>shadowsocks-windows</code></a></p>
</li>
<li>
<p>Mac OS<br>
<a href="https://github.com/shadowsocks/ShadowsocksX-NG"><code>ShadowsocksX-NG</code></a></p>
</li>
<li>
<p>Android<br>
<a href="https://github.com/shadowsocks/shadowsocks-android"><code>shadowsocks-android</code></a><br>
<a href="https://github.com/shadowsocksrr/shadowsocksr-android"><code>shadowsocksr-android</code></a></p>
</li>
<li>
<p>iOS<br>
<a href="https://itunes.apple.com/hk/app/shadowrocket/id932747118?l=en&amp;mt=8"><code>ShadowRocket</code></a></p>
</li>
</ul>
<blockquote>
<p>ShadowRocket 已在国区下架，请自行于其他地区 App Store 购买，或寻找其他替代 App</p>
</blockquote>
<ul>
<li>各平台的客户端列表<br>
<a href="https://shadowsocks.org/en/download/clients.html"><code>shadowsocks</code></a></li>
</ul>
<h1 id="shadowsocks线路代理商">Shadowsocks线路代理商</h1>
<p>如上所述，Shadowsocks 需要服务器端&amp;客户端的部署+连接才能达到应有的效果，所以我们需要选择各种服务商提供的代理配置。</p>
<p>供 Shadowsocks 所用的代理有免费的也有付费的。免费的不一定慢，付费的也并不一直稳定。只是付费代理在出现线路问题后会及时调整。</p>
<blockquote>
<p>由于本文作者使用的代理商有邀请奖励，所以请有需要而且找得到作者的小伙伴来找我要个邀请吧。。。</p>
</blockquote>
<h1 id="shadowsocksshadowsocksr客户端添加服务器配置">Shadowsocks/ShadowsocksR客户端添加服务器配置</h1>
<p>Shadowsocks/ShadowsocksR 客户端使用极其简便，且基本上看选项就能完全明白什么意思。这里仅以作者使用的平台做简要示范。</p>
<h2 id="获取服务器代理配置">获取服务器代理配置</h2>
<blockquote>
<p>请在各自使用的免费/付费代理商处获得。此处展示二维码添加配置示范。</p>
</blockquote>
<p>在代理商网站找到类似下图的菜单：</p>
<figure data-type="image" tabindex="1"><img src="http://lx-public-pic.oss-cn-shanghai.aliyuncs.com/Shadowsocks/%E5%B1%8F%E5%B9%95%E5%BF%AB%E7%85%A7%202019-03-11%2011.06.38.png" alt=""></figure>
<p>点击类似<code>我的账号</code>的按钮，进入类似下图的界面：</p>
<figure data-type="image" tabindex="2"><img src="http://lx-public-pic.oss-cn-shanghai.aliyuncs.com/Shadowsocks/%E5%B1%8F%E5%B9%95%E5%BF%AB%E7%85%A7%202019-03-11%2011.10.07.png" alt=""></figure>
<p>点击类似<code>查看详细信息（增加服务器节点）</code>的链接，进入类似下图的界面：</p>
<figure data-type="image" tabindex="3"><img src="http://lx-public-pic.oss-cn-shanghai.aliyuncs.com/Shadowsocks/%E5%B1%8F%E5%B9%95%E5%BF%AB%E7%85%A7%202019-03-11%2011.11.40.png" alt=""></figure>
<p>点击类似<code>查看详细信息（扫描二维码）</code>的链接，进入类似下图的界面：</p>
<figure data-type="image" tabindex="4"><img src="http://lx-public-pic.oss-cn-shanghai.aliyuncs.com/Shadowsocks/%E5%B1%8F%E5%B9%95%E5%BF%AB%E7%85%A7%202019-03-11%2011.12.37.png" alt=""></figure>
<p>点击类似<code>显示XXX二维码</code>的按钮，屏幕上出现二维码，此时转去操作Shadowsocks/ShadowsocksR客户端。</p>
<h2 id="shadowsocksshadowsocksr客户端操作">Shadowsocks/ShadowsocksR客户端操作</h2>
<p>安装并运行客户端App后，请在众多状态栏/任务栏图标中找到软件图标（图标形状可理解为三角形、纸飞机等）：</p>
<figure data-type="image" tabindex="5"><img src="http://lx-public-pic.oss-cn-shanghai.aliyuncs.com/Shadowsocks/%E5%B1%8F%E5%B9%95%E5%BF%AB%E7%85%A7%202019-03-11%2011.14.04.png" alt=""></figure>
<p>鼠标左键或右键单击（请自行尝试）后弹出类似下图的菜单：</p>
<figure data-type="image" tabindex="6"><img src="http://lx-public-pic.oss-cn-shanghai.aliyuncs.com/Shadowsocks/%E5%B1%8F%E5%B9%95%E5%BF%AB%E7%85%A7%202019-03-11%2011.14.29.png" alt=""></figure>
<blockquote>
<p>上图所示<code>PAC模式</code>为自动代理模式，仅需要代理的流量才走代理服务器；<code>全局模式</code>即接管全部流量。</p>
</blockquote>
<p>请在类似<code>服务器配置</code>的选项中找到类似下图的选项：</p>
<figure data-type="image" tabindex="7"><img src="http://lx-public-pic.oss-cn-shanghai.aliyuncs.com/Shadowsocks/%E5%B1%8F%E5%B9%95%E5%BF%AB%E7%85%A7%202019-03-11%2011.15.33.png" alt=""></figure>
<p><strong>扫描屏幕上的二维码即可。</strong></p>
<p>请自行在菜单中找到类似<code>更新PAC</code>的选项，经常更新PAC规则可以保持<code>智能代理</code>：</p>
<figure data-type="image" tabindex="8"><img src="http://lx-public-pic.oss-cn-shanghai.aliyuncs.com/Shadowsocks/%E5%B1%8F%E5%B9%95%E5%BF%AB%E7%85%A7%202019-03-11%2011.16.04.png" alt=""></figure>
<hr>
<p><em><strong>请注意本文高频使用的<code>类似</code>，意即与各自实际情况类比即可</strong></em></p>
<p><a href="#top">Back to Top</a></p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Hello Gridea]]></title>
        <id>https://ntlx.github.io/post/hello-gridea</id>
        <link href="https://ntlx.github.io/post/hello-gridea">
        </link>
        <updated>2018-12-11T16:00:00.000Z</updated>
        <summary type="html"><![CDATA[<p>👏  欢迎使用 <strong>Gridea</strong> ！<br>
✍️  <strong>Gridea</strong> 一个静态博客写作客户端。你可以用它来记录你的生活、心情、知识、笔记、创意... ...</p>
]]></summary>
        <content type="html"><![CDATA[<p>👏  欢迎使用 <strong>Gridea</strong> ！<br>
✍️  <strong>Gridea</strong> 一个静态博客写作客户端。你可以用它来记录你的生活、心情、知识、笔记、创意... ...</p>
<!-- more -->
<p><a href="https://github.com/getgridea/gridea">Github</a><br>
<a href="https://gridea.dev/">Gridea 主页</a><br>
<a href="http://fehey.com/">示例网站</a></p>
<h2 id="特性">特性👇</h2>
<p>📝  你可以使用最酷的 <strong>Markdown</strong> 语法，进行快速创作</p>
<p>🌉  你可以给文章配上精美的封面图和在文章任意位置插入图片</p>
<p>🏷️  你可以对文章进行标签分组</p>
<p>📋  你可以自定义菜单，甚至可以创建外部链接菜单</p>
<p>💻  你可以在 <strong>Windows</strong>，<strong>MacOS</strong> 或 <strong>Linux</strong> 设备上使用此客户端</p>
<p>🌎  你可以使用 <strong>𝖦𝗂𝗍𝗁𝗎𝖻 𝖯𝖺𝗀𝖾𝗌</strong> 或 <strong>Coding Pages</strong> 向世界展示，未来将支持更多平台</p>
<p>💬  你可以进行简单的配置，接入 <a href="https://github.com/gitalk/gitalk">Gitalk</a> 或 <a href="https://github.com/SukkaW/DisqusJS">DisqusJS</a> 评论系统</p>
<p>🇬🇧  你可以使用<strong>中文简体</strong>或<strong>英语</strong></p>
<p>🌁  你可以任意使用应用内默认主题或任意第三方主题，强大的主题自定义能力</p>
<p>🖥  你可以自定义源文件夹，利用 OneDrive、百度网盘、iCloud、Dropbox 等进行多设备同步</p>
<p>🌱 当然 <strong>Gridea</strong> 还很年轻，有很多不足，但请相信，它会不停向前 🏃</p>
<p>未来，它一定会成为你离不开的伙伴</p>
<p>尽情发挥你的才华吧！</p>
<p>😘 Enjoy~</p>
]]></content>
    </entry>
</feed>